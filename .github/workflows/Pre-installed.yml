name: Pre-installed
on: push

jobs:
  check-tools:
    runs-on: ubuntu-latest
    steps:
      - name: Check Versions
        run: |
          echo "Docker version:"
          docker --version
          echo "Python version:"
          python3 --version
          echo "Node version:"
          node --version
          echo "Git version:"
          git --version
